import { RuleSetRule } from 'webpack';
import { MediaOptions } from './types';
export declare const mediaDefaultOptions: MediaOptions;
declare const _default: (options?: MediaOptions) => {
    rules: RuleSetRule[];
};
export default _default;
